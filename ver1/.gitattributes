# Keep LF endings for files that run inside Linux containers / shells.
# Without this, Git on Windows (autocrlf) can corrupt the entrypoint script.
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
Dockerfile text eol=lf
*.conf text eol=lf
