$kind: http-request
name: Suggest Doctor (Patient)
method: GET
url: '{{baseUrl}}/chatbot/suggest-doctor?department_id={{departmentId}}&appointment_date={{appointmentDate}}'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
