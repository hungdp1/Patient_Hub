$kind: http-request
name: Delete Procedure
method: DELETE
url: '{{baseUrl}}/library/procedures/{{procedureId}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
