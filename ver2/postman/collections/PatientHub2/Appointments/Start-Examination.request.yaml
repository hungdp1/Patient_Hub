$kind: http-request
name: Start Examination
method: POST
url: '{{baseUrl}}/appointments/{{appointmentId}}/start'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
