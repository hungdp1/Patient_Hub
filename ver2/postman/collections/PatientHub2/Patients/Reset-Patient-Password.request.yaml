$kind: http-request
name: Reset Patient Password
method: POST
url: '{{baseUrl}}/patients/{{patientId}}/reset-password'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
