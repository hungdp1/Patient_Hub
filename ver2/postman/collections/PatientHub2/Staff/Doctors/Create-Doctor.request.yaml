$kind: http-request
name: Create Doctor
method: POST
url: '{{baseUrl}}/staff/doctors'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "username": "dr_nguyen",
      "password": "password123",
      "full_name": "Dr. Nguyen Van A",
      "department_id": "{{departmentId}}"
    }
